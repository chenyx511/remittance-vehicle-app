# 可选：上传服务后端接口（建议 HTTPS）
# 未配置时，系统会回退到前端本地存储模式（仅当前浏览器可见）
VITE_UPLOAD_API_URL=

# 可选：Supabase 服务端存储（配置后数据在多端共享，取代 localStorage）
# 在 Supabase 控制台创建项目后，将 URL 和 anon key 填入
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
